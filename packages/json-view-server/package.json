{
  "name": "json-view-server",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "ts": "tsx",
    "dev": "pnpm ts src/main.ts",
    "generate": "pnpm exec generate edgeql-js && pnpm exec generate interfaces",
    "check": "tsc"
  },
  "dependencies": {
    "@trpc/server": "^10.10.0",
    "express": "^4.18.2",
    "zod": "^3.20.2"
  },
  "devDependencies": {
    "@edgedb/generate": "^0.0.7",
    "@types/express": "^4.17.16",
    "@types/node": "^18.11.19",
    "typescript": "^4.8.3"
  }
}