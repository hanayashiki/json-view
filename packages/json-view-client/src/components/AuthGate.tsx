// import { useAuth } from "../utils/auth";
// import { trpc } from "@/json-view-client/src/utils/trpc";

export function AuthGate(_props: React.PropsWithChildren) {
  //mconst { token } = useAuth();

  // const _meQuery = trpc.me.useQuery(undefined, {
  //   enabled: !!token,
  // });

  return <div></div>;
}
